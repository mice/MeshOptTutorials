using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SubMeshSelect : MonoBehaviour
{
    void Start()
    {
        var meshRenderer = this.GetComponent<MeshRenderer>();
        if (meshRenderer != null)
        {
            
        }
    }
}
